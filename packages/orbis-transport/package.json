{
  "name": "@orbisapp/transport",
  "version": "2.0.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "pnpm run typecheck && pnpm run test",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@hpke/chacha20poly1305": "^1.8.0",
    "@hpke/common": "^1.10.1",
    "@hpke/core": "^1.9.0",
    "@hpke/dhkem-x25519": "^1.8.0",
    "ip-address": "^10.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.x",
    "typescript": "rc",
    "vitest": "^4.1.10"
  }
}
