{
  "name": "@orbisapp/remote-agent-node-store",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "pnpm run typecheck && pnpm run test",
    "test": "vitest run src",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@orbisapp/orbis-agent-backend": "workspace:*",
    "@orbisapp/remote-agent-protocol": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.x",
    "typescript": "rc",
    "vitest": "^4.1.10"
  }
}
